{
  "name": "lebanon-geo-dashboard",
  "version": "1.0.0",
  "description": "Interactive demographic geo-dashboard for Lebanon with AI-assisted analysis",
  "private": true,
  "scripts": {
    "dev": "npx netlify dev",
    "serve": "npx netlify dev --offline",
    "test": "node tests/run.js"
  },
  "dependencies": {
    "@google/generative-ai": "^0.15.0"
  }
}
